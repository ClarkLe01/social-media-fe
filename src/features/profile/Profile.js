import React from 'react';

import CreatePost from '@common/components/CreatePost';

const Profile = () => {
    return (
        <>
            <CreatePost />
            <CreatePost />
            <CreatePost />
            <CreatePost />
            <CreatePost />
            <CreatePost />
        </>
    );
};

export default Profile;